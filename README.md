# Google-forms-MCQ-getter
get all the MCQ in JSON by passing a link
# Api that returns all the MCQ question when google form link is passed to it as  a post request
you can send your post request as {"examLink":google froms link }
and it will return all the mcq question and there respective option in JSON
